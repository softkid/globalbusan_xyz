// Legacy StripePayment tests removed; Sui-only build placeholder.
import React from 'react'

describe('StripePayment (deprecated)', () => {
  it('is deprecated in Sui-only build', () => {
    expect(true).toBe(true)
  })
})

